name: bnk-dataexport
channels:
  - conda-forge
dependencies:
  - python>=3.12
  - matplotlib
  - osmnx>=2
  - tqdm
  - geojson

  - pyrosm>=0.13.1
  - jupyterlab
  - pre-commit
  - superblockify

  # BikeNetKit
  - pip: # pip because conda takes days to update version
    - growbikenet
    - fixbikenet
    - linkbikenet
